# Neo4j driver
neo4j==6.1.0

# Ollama (Python client for local LLMs / embeddings)
ollama==0.6.1

# OpenRouter (OpenAI-compatible API for RAG chat model)
openai>=1.0.0

# PDF text extraction
pypdf>=4.0.0

# Testing
pytest>=8.0.0


docling>=2.9.0
docling-core[chunking]>=2.8.0
transformers>=4.40.0
